{
  "name": "lacrossehub-scraper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fetches public PLL stats with a headless browser and writes ../data.json for LacrosseHub.",
  "scripts": {
    "scrape": "node scrape.mjs",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.47.0"
  }
}
